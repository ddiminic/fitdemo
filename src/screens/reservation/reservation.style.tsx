import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  container: {
    paddingTop: 50,  
    display: "flex",
    flexDirection: "column",
    alignItems: "center"
  },
  datePick: {
      paddingTop: 30,
      paddingBottom: 30,
  }
});