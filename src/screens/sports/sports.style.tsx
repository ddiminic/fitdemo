import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  container: {
    alignItems: "center",
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-around",
    flexWrap:"wrap",
  }
});